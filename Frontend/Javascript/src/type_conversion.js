/**
 * Demonstrating Type Conversion in JavaScript.
 */

// Define a string representing a year
const inputYear = "1994";
console.log(typeof inputYear); // Output: string

// Convert the string to a number
const newYear = Number(inputYear);
console.log(typeof newYear); // Output: number
