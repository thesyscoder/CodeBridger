/**
 * Represents a JavaScript variable for storing a value.
 * @type {string}
 */
let js = "Awesome";

// Output the value of the variable 'js' to the console
console.log(js);

/**
 * Represents a variable storing a person's name.
 * @type {string}
 */
let name = "James";

// Output the value of the variable 'name' to the console
console.log(name);

// Practice variables and their respective values

/**
 * Represents the name of a country.
 * @type {string}
 */
let country = "India";

/**
 * Represents the capital city of a country.
 * @type {string}
 */
let capital = "Delhi";

// Output the values of the variables 'country' and 'capital' to the console
console.log(country);
console.log(capital);
