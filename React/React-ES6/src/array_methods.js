/**
 * .map() most used array method.
 * The .map() methods allow you run a function on each item in the array, returning a new array as the result.
 */

// declare array

const bikes = ["Yamaha", "KTM", "Kawasaki", "BMW"];

// use map function
bikes.map((bike) => {
  console.log(bike);
});
