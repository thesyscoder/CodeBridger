/**
 * The ternary operator is a simplified conditional operator like if / else.
 * @syntax condition ? true:false
 * @file ternary_operator.js
 * @author thesyscoder
 */

// check the condition

const age = 17;

let result = age >= 18 ? "Can Vote" : "Cant vote";
console.log(result);
